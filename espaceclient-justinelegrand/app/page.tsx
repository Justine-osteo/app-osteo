import Loading from 'components/loading';

export default function Home() {
    // On affiche simplement ton composant de chargement existant
    // en attendant que le Middleware redirige l'utilisateur
    return <Loading />;
}